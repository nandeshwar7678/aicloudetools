name: Auto Deploy to GoDaddy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: aicloudetools.com
          username: kpbisslzozf2
          password: Akashya@12
          protocol: ftps
          port: 21
          server-dir: /public_html/