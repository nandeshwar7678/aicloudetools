name: Auto Deploy to GoDaddy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.aicloudetools.com
          username: kpbisslzozf2
          password: Akashya@12
          protocol: ftp
          port: 21
          passive: true
          server-dir: /public_html/